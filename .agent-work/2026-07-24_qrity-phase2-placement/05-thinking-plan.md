# 05 — Implementation plan

1. Generalize traversal from template dimension while validating exact canonical input.
2. Enforce binary message vectors and exact encoding-region length.
3. Preserve existing return shape and map bits only to prior `:unset` cells.
4. Add semantic placed-matrix/placement specs and fdefs.
5. Compare all 40 coordinate streams with an independent reference.
6. Place real complete messages for all 160 profiles and prove recovery/preservation.
7. Run JVM, Node, Babashka, interoperability, and independent reviews.
8. Document the pre-metadata/unmasked boundary and commit separately.
