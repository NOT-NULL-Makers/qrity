# Second Opinion — metadata boundary

- **Author:** Second Opinion worker `/root/phase2_code_inventory`.
- **Independence:** context fresh · worker separate · model family unknown.
- **Artifact transport:** exact-return relay; Coordination persisted this summary.

## Recommendation integrated

Proceed after reframing the work as pure metadata calculation and atomic reservation
resolution, not as an assertion that metadata precedes generalized masking.

- Infer version only from an exact metadata-ready construction matrix.
- Make error-correction level and mask reference explicit.
- Preserve the existing implicit-M calculator arity.
- Reject unresolved data, partial metadata, completed metadata, and malformed matrices.
- Avoid a new public partial version writer; write both fields atomically.
- Document that the low-level mask argument cannot prove how encoding modules were
  actually masked. Later orchestration must bind masking and metadata.

Success evidence should cover every Annex C/D word, both physical orientations, all
1,280 version/level/mask matrix combinations, structured failures, fixed output, and
all three supported runtimes.
