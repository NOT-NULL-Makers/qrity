# Validated intent

Resume the approved mode-expansion plan after assertion maintenance.

This checkpoint completes ordinary QR single-segment Alphanumeric generation:

- `0010` mode indicator;
- 9/11/13-bit character-count fields;
- selected-profile termination, alignment, and padding;
- smallest-version selection;
- complete symbol orchestration through the existing shared tail;
- renderer and independent decoder interoperability evidence.

The public surface remains explicitly mode-specific and provisional:
`encode-alphanumeric` is added beside `encode-numeric`. A stable generic `encode`,
Byte mode, mixed segmentation, ECI, FNC1, Structured Append, Kanji, Micro QR, and
Model 1 are excluded.

Commit this checkpoint and stop for human approval before Byte mode.
