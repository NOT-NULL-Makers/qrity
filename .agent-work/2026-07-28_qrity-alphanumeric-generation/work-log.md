# Work log

## 2026-07-28

- Human approved resuming the mode-expansion plan after assertion maintenance.
- Coordination bounded this checkpoint to complete ordinary single-segment
  Alphanumeric generation, with a commit and approval stop before Byte mode.
- Context Gathering, Second Opinion, interoperability mapping, Code Review, and
  Testing Review used bounded workers. Direct scoped persistence was used only
  for the two new test namespaces; other delegated artifacts used exact-return
  relay. Root `/root`, parent `/root`, owner `/root`, depth 1/max 1, no
  subdelegation. Effective model/effort were inherited or unknown; no validated
  realization was claimed. Cross-family review remained unavailable under the
  human's Fable limit.
- Implementation reused canonical Table 5 packing, mode-independent padding,
  catalogue selection, and the existing private final-symbol tail.
- Added the explicit provisional `encode-alphanumeric` API rather than a
  premature generic `encode`.
- Independent segment tests cover all 160 profiles and every terminator length.
- Complete-symbol tests cover count-width/version transitions, every level,
  mask selection, provenance, renderer compatibility, errors, and Numeric
  regression.
- Code Review found a default Numeric verifier compatibility regression and
  stale parameter documentation. Both were corrected; targeted rereview was
  clean.
- The first full ClojureScript run exposed a test-only `1.0`/`1` JavaScript
  number equivalence. The invalid fixture became `1.5`; targeted Testing
  rereview accepted it.
- Final JVM, Babashka, and ClojureScript suites each passed 114 tests / 6,328
  assertions with zero failures and zero errors.
- Alphanumeric interoperability passed 18 runtime artifacts, 6 byte-identical
  triples, and 36 exact decoder checks. The restored default Numeric harness
  passed 15 artifacts, 5 triples, and 30 decoder checks.
- After the final Python compatibility hardening, a fresh Alphanumeric
  interoperability run again passed all 18 artifacts, 6 runtime triples, and
  36 decoder checks; evidence is in
  `/tmp/qrity-alphanumeric-interop-gkeyvaui`.
- Independent Final Review accepted the checkpoint with no correctness findings
  and authorized the scoped commit while requiring the stop before Byte mode.
