# Implementation

Author: `/root/generalized_numeric_implementation`
Role: Implementation proposal
Artifact transport: exact-return relay; Coordination persisted the bounded change
Lineage: owner `/root`, parent `/root`, depth 1, maximum depth 1
Model/reasoning selection: no validated deployment binding was supplied;
effective pair inherited or unknown

The worker proposed an additive private composition:

`smallest-numeric-version`
→ `numeric-data-codewords`
→ `construct-final-message`
→ `function-matrix` / `place-data`
→ `select-best-candidate`
→ minimal five-key symbol.

It required:

- preserving `encode-numeric-v1-m`;
- keeping candidate penalties outside the symbol;
- validating runtime arguments before composition;
- separating structural shape from exact re-encoding provenance;
- testing all levels, leading zeros, capacity transitions, mask minima, fixed
  compatibility, and structured errors.

Coordination implemented that proposal with one refinement: the structural predicate
is deliberately exception-safe for arbitrary values and checks only binary
dimensions, not matrix provenance. A same-shaped bit flip remains structurally valid
and fails the input-relative predicate.

The permanent harness was implemented additively with a separate generalized emitter
and scripts. The existing pair-based fixed emitter protocol remains unchanged.
