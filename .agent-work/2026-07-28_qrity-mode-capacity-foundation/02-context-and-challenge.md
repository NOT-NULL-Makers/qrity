# Context and challenge

Authors: `/root/mode_expansion_inventory`, `/root/mode_expansion_challenge`

Roles: Context Gathering and Second Opinion

Artifact transport: exact-return relay; Coordination persists

Lineage: owner `/root`, parent `/root`, depth 1, maximum depth 1

Model/reasoning selection: no validated deployment binding was supplied;
effective pair inherited or unknown.

Both reviews support Alphanumeric before Byte and identify the current
mode-independent tail:

`construct-final-message → function-matrix/place-data → select-best-candidate`.

They recommend a narrow private extraction rather than copied mode-specific
orchestration or a speculative segment planner.

Relevant ISO facts:

- Alphanumeric count widths: 9/11/13 for Versions 1–9/10–26/27–40;
- Byte count widths: 8/16/16;
- Table 7 supplies printed capacities for every version/level profile.

The Byte contract remains a later decision. The preferred eventual design is a
canonical vector of octets with a separate default-ECI ISO-8859-1 text adapter.
This stage records no public Byte API.
